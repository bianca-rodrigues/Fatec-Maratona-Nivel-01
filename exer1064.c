#include <stdio.h>
 
int main() 
{
    float media, num1, num2, num3, num4, num5, num6;
    int posit=0;

    scanf("%f %f %f %f %f %f", &num1, &num2, &num3, &num4, &num5, &num6);

    if (num1>0)
    {
        
    }
    
    

    


    printf("%i valores positivos\n", posit);
    printf("%.1f\n", media);

 
    return 0;
}